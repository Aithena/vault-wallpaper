# API (copy to apps/api/.dev.vars for local wrangler)
JWT_SECRET=change-me-in-production
EMAIL_FROM=noreply@example.com
# RESEND_API_KEY=
# XUNHUPAY_APPID=
# XUNHUPAY_APPSECRET=
# XUNHUPAY_GATEWAY=https://api.xunhupay.com

# Local tip:
#   pnpm dev:api         → wrangler --local (isolated, no cloud KV quota)
#   pnpm dev:api:remote  → cloud KV/R2 (shares free-tier caps with production)

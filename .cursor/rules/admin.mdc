---
description: 管理端约定：布局壳、权限树、Element Plus、后台 API。
globs: apps/admin/**,docs/admin-*.md
alwaysApply: false
---

# 管理端（`apps/admin`）

壳：`layouts/AdminLayout.vue`（顶栏 A + 一级 C + 二级 E + 内容 F）。菜单不要手写一份：`src/config/menu.ts` 映射 `@vault/shared` 的 `ADMIN_PERMISSION_TREE`。改信息架构先改 `docs/admin-ia.md` 和权限树，再改路由。

UI：Element Plus + auto-import（不要重复手动 import 组件）。主题变量在 `src/styles/admin.less`。权限：`src/lib/permission.ts` 的 `hasMenu` / `hasButton`。

请求：`src/lib/api.ts` → `vault_admin_token`，前缀 `/api/admin`。登录 `POST /api/admin/auth/login`。本地空库账号 `admin` / `admin123`。

路由表：`src/router/index.ts`。业务页在 `src/views/` 按模块分目录；不要读 `src/mock/`（若仍存在则视为过时）。
